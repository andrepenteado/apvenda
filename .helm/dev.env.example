# Modelo das variáveis de ambiente de DEV do backend.
# Copie para .helm/backend.env (git-ignored) e preencha.
# Usado pelo run config do IntelliJ (Backend UP -> envFilePaths).
VENDA_DB_PASSWORD=dev-local
VENDA_OIDC_CLIENT_SECRET=<client-secret-do-client-LOCAL-venda-no-SSO>
